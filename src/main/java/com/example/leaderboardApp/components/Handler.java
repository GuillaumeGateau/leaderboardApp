package com.example.leaderboardApp.components;

/**
 * Created by williamke on 7/17/15.
 */
public class Handler {

    /*
    public appUpdate(int hipchat_id, String name, int dips) {
        hex map lookup (hipchat_id)
        ***need to add if null here and do Competitor.create(hipchat_id, name)
        ***if it already exists, then we just continue
        Competitor.addReps(dips);       //this adds the dips
        Competitor.getScore();          //this will return the score
        Competitor.getProgress();       //this will return the progress
        Competitor.writeToDB(reps);     //this writes the data to the db
    }

    public appUpdate(int hipchat_id, String name) {
        hex map lookup (hipchat_id)
        ***need to add if null here and do Competitor.create(hipchat_id, name)
        ***if it already exists, then we should do Competitor.setName(name)
    }
     */

}
